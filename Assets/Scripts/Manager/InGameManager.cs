using Enum;
using UnityEngine;

namespace Manager
{
    public class InGameManager : MonoBehaviour
    {
        private void OnStateChanged(InGameEnum.State state)
        {
            
        }
    }
}